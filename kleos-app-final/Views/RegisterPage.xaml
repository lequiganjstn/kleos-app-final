<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="kleos_app_final.Views.RegisterPage"
             Title="Register"
             BackgroundColor="#f9f9f9">

    <VerticalStackLayout Padding="20" Spacing="12" VerticalOptions="Center">
        
        <!-- Header -->
        <Label Text="Create Account"
               FontFamily="InstrumentSerif"
               FontSize="32"
               TextColor="#000000"
               HorizontalTextAlignment="Center"
               Margin="0,0,0,24" />

        <!-- Name Field -->
        <Entry x:Name="NameEntry"
               Placeholder="Full Name"
               Text="{Binding Name}"
               PlaceholderColor="#7e7576"
               FontFamily="Inter"
               FontSize="16"
               TextColor="#1a1c1c"
               Margin="0,8,0,8" />

        <!-- Email Field -->
        <Entry x:Name="EmailEntry"
               Placeholder="Email"
               Text="{Binding Email}"
               Keyboard="Email"
               PlaceholderColor="#7e7576"
               FontFamily="Inter"
               FontSize="16"
               TextColor="#1a1c1c"
               Margin="0,8,0,8" />

        <!-- Password Field -->
        <Entry x:Name="PasswordEntry"
               Placeholder="Password (min 8 characters)"
               Text="{Binding Password}"
               IsPassword="True"
               PlaceholderColor="#7e7576"
               FontFamily="Inter"
               FontSize="16"
               TextColor="#1a1c1c"
               Margin="0,8,0,8" />

        <!-- Confirm Password Field -->
        <Entry x:Name="ConfirmPasswordEntry"
               Placeholder="Confirm Password"
               Text="{Binding ConfirmPassword}"
               IsPassword="True"
               PlaceholderColor="#7e7576"
               FontFamily="Inter"
               FontSize="16"
               TextColor="#1a1c1c"
               Margin="0,8,0,8" />

        <!-- Error Message -->
        <Label Text="{Binding ErrorMessage}"
               FontFamily="Inter"
               FontSize="14"
               TextColor="#ba1a1a"
               IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}"
               Margin="0,12,0,0" />

        <!-- Register Button -->
        <Button Text="Create Account"
                Command="{Binding RegisterCommand}"
                FontFamily="Inter"
                FontSize="16"
                FontAttributes="Bold"
                TextColor="#ffffff"
                BackgroundColor="#000000"
                CornerRadius="0"
                Padding="0,16"
                Margin="0,24,0,0" />

        <!-- Loading Indicator -->
        <ActivityIndicator IsRunning="{Binding IsLoading}"
                          IsVisible="{Binding IsLoading}"
                          Color="#000000"
                          Margin="0,12,0,0" />

        <!-- Link to Login -->
        <StackLayout Orientation="Horizontal" HorizontalOptions="Center" Margin="0,24,0,0">
            <Label Text="Already have an account? "
                   FontFamily="Inter"
                   FontSize="14"
                   TextColor="#4c4546" />
            <Label Text="Login"
                   FontFamily="Inter"
                   FontSize="14"
                   FontAttributes="Bold"
                   TextColor="#000000">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding GoToLoginCommand}" />
                </Label.GestureRecognizers>
            </Label>
        </StackLayout>

    </VerticalStackLayout>

</ContentPage>
